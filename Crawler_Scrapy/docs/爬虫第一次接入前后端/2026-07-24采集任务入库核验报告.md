# 2026-07-24 采集任务入库核验报告

> 文档性质：本文是 2026-07-24 当日数据和当时代码的历史验证证据，不是当前数据库数量或当前运行逻辑说明。
> 当前基准：Schema、migration、接口、分阶段超时、补偿和漏斗口径以《数据库修改与接口变更汇总说明》为准；查询新任务使用《前端采集数据数据库验证说明》。

## 0. 后续实现对本报告的影响

1. 本报告中的任务 22 是旧版“整个任务共用一个超时计时器”造成的历史故障。当前已改为 `FETCH` 使用任务超时、`PARSE/DEDUPLICATE/STORE` 每阶段至少 10 分钟、`MATCH` 至少 30 分钟。
2. 任务 22 的两条缺失公告后续已由补偿任务 58 复用 JSON 完成抽取和项目公告匹配；本文保留修复前状态，不应用来判定当前仍缺失。
3. 本报告第 7 节的“直接累加全库 `crawl_task_stage.success_count`”是当时口径。当前会先按 `run_config.retryOfTaskId` 合并原任务与补偿任务，再对每个阶段取任务链最大成功数。
4. 文中任务数、公告数、表行数和 ID 都只有历史取证价值，不应复制到当前概览或 API 文档。

## 1. 核验范围

- 日期口径：北京时间 `2026-07-24 00:00:00` 至次日 `00:00:00`。
- 数据源：华新阳光采购平台 `data_source.id=6`、玖邦招标采购电子交易平台 `data_source.id=14`。
- 排除项：带 `run_config.demoException=true` 的异常展示模拟任务。
- 核验脚本：`api/scripts/verifyTodayCrawlPipeline.js`，只读数据库，不修改任务或业务数据。

运行方式：

```bash
cd /home/intsig/ProjectRecommendationSystem/api
npm run verify:crawl-pipeline
```

核验指定日期：

```bash
npm run verify:crawl-pipeline -- --date=2026-07-24
```

## 2. 总体结论

今天真实任务共 40 个，其中成功 28 个、取消 8 个、失败 2 个、超时 2 个。28 个成功任务中有 22 个没有抓到符合时间和栏目条件的新 JSON，后续阶段按设计记为 `SKIPPED`，不属于入库失败。

今天共采集抓取 21 条任务公告：

- 19 条完整经过解析、去重、结构化存储和项目匹配；
- 2 条属于任务 22，在采集成功后进入解析阶段时因任务总超时被终止，只写入原始层，没有生成抽取结果和项目关联；
- 因此“今天抓到的全部数据都完整进入业务层”的结论为否，完整率为 `19 / 21 = 90.48%`；
- 除任务 22 外，其余所有有数据的成功任务均完整通过 `MATCH`。

## 3. 漏斗结果

| 阶段 | 成功数 | 说明 |
| --- | ---: | --- |
| `FETCH` 采集抓取 | 21 | JSON 公告条数 |
| `PARSE` 解析提取 | 19 | 两条在解析导入过程中超时 |
| `DEDUPLICATE` 去重清洗 | 19 | 19 条均建立任务公告关系 |
| `STORE` 结构化存储 | 19 | 19 条均有 `notice_extraction` |
| `MATCH` 智能匹配 | 19 | 19 条均关联 `project_notice` |
| `RECOMMEND` 推荐输出 | 0 | 当前明确跳过，不写 `project_requirement` |

## 4. 各表实际保存结果

| 数据阶段 | 表 | 本次关联数量 | 结果说明 |
| --- | --- | ---: | --- |
| 任务主记录 | `crawl_task` | 40 | 保存采集源、任务类型、状态、参数、时间、输出路径和总数 |
| 六阶段状态 | `crawl_task_stage` | 240 | 每个任务 6 条阶段记录 |
| 运行日志事件 | `crawl_task_event` | 349 | 保存创建、启动、阶段完成、失败和结束事件 |
| 任务发现关系 | `crawl_task_notice` | 21 | 表达本任务发现了哪条原始公告 |
| 原始公告 | `raw_notice` | 21 个不同公告 | 其中 12 条为任务首次发现，9 条为已有公告再次发现 |
| 原始附件 | `raw_notice_attachment` | 0 | 本次 21 条公告没有落入原始附件记录，不是阶段失败 |
| 公告抽取 | `notice_extraction` | 19 | 任务 22 的两条公告缺失 |
| 业务公告 | `project_notice` | 19 | 19 条抽取结果全部完成业务公告关联 |
| 项目 | `project` | 19 个关联项目 | 通过 `project_notice.project_id` 关联，不代表都新增了项目 |
| 业务附件 | `project_notice_attachment` | 0 | 本次关联公告没有业务附件记录 |
| 项目需求 | `project_requirement` | 0 | 按当前范围明确不写入 |

## 5. 有数据任务明细

| 任务 ID | 数据源 | 触发方式 | 状态 | 抓取 | 解析 | 去重 | 存储 | 匹配 | 是否完整 |
| ---: | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 7 | 华新 | 手动 | 成功 | 1 | 1 | 1 | 1 | 1 | 是 |
| 8 | 玖邦 | 手动 | 成功 | 1 | 1 | 1 | 1 | 1 | 是 |
| 9 | 华新 | 异常重试 | 成功 | 1 | 1 | 1 | 1 | 1 | 是 |
| 10 | 华新 | 手动 | 成功 | 6 | 6 | 6 | 6 | 6 | 是 |
| 12 | 华新 | 手动 | 成功 | 1 | 1 | 1 | 1 | 1 | 是 |
| 22 | 华新 | 手动 | 超时 | 2 | 0 | 0 | 0 | 0 | 否 |
| 24 | 玖邦 | 手动 | 成功 | 9 | 9 | 9 | 9 | 9 | 是 |

## 6. 两条未完成解析的公告

任务 22 的 `FETCH` 阶段成功数为 2，随后 `PARSE` 阶段报错 `npm exited with code null (SIGTERM)`。任务配置的总超时时间为 1 分钟，API 在解析导入过程中终止了子进程。

| `raw_notice.id` | 来源公告 ID | 公告标题 | 当前落库情况 |
| ---: | --- | --- | --- |
| 3861 | 2079036931560267776 | 太原天然气有限公司太原市热力集团有限责任公司杨家峪燃气调峰热源厂、中北高新区上兰能源岛项目燃气设备采购(002标段)中标结果公示 | 已有 `raw_notice` 和 `crawl_task_notice`，缺少 `notice_extraction/project_notice` |
| 3862 | 2079036994277695488 | 太原天然气有限公司太原市热力集团有限责任公司杨家峪燃气调峰热源厂、中北高新区上兰能源岛项目燃气设备采购(003标段)中标结果公示 | 已有 `raw_notice` 和 `crawl_task_notice`，缺少 `notice_extraction/project_notice` |

这两条 `raw_notice.parse_status` 显示为 `PARSED`，含义只是原始 JSON 已被原始公告导入器接受，不代表已经存在 `notice_extraction`。判断“解析提取完成”必须同时检查 `notice_extraction`，不能只看 `raw_notice.parse_status`。

## 7. 当时漏斗全库差值补充

截至本报告执行时，管理端漏斗按 `crawl_task_stage.success_count` 全库直接聚合：

- `FETCH - PARSE = 2`：全部来自今天的任务 22；
- `PARSE - DEDUPLICATE = 0`；
- `DEDUPLICATE - STORE = 0`；
- `STORE - MATCH = 1`：来自 7 月 16 日导入的旧任务 2，1153 条中有 1 条未建立项目公告关系，与今天任务无关；
- `RECOMMEND = 0`：推荐阶段当前固定跳过。

## 8. 定时计划恢复规则

暂停计划再次启用时：

- 原 `next_run_at` 仍在未来：保留原时间；
- 原 `next_run_at` 已经过期：不立即补跑已错过的批次，改为 `启用时间 + interval_minutes`；
- 后续调度仍按新的 `next_run_at` 和固定频率继续推进。
